

export const metadata = {
    title: 'Dashboard: Manage Block Residences',
};


export default function Layout({ children }: { children: any }) {
    return (
        <>
            {children}
        </>
    );
}
