

export const metadata = {
    title: 'Dashboard: Services detail',
};


export default function Layout({ children }: { children: any }) {
    return (
        <>
            {children}
        </>
    );
}
